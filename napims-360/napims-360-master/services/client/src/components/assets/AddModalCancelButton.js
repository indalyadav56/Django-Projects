import React from "react"
import { Button } from "reactstrap"

function AddModalCancelButton(props) {
  return (
    <Button color="secondary" outline {...props}>
      Cancel
    </Button>
  )
}

export default AddModalCancelButton